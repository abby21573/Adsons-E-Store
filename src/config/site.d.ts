export type SiteConfig = typeof siteConfig;
export declare const siteConfig: {
    name: string;
    description: string;
    navItems: {
        label: string;
        href: string;
    }[];
    navMenuItems: {
        label: string;
        href: string;
    }[];
};
